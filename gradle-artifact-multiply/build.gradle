plugins {
    id 'java'
}

apply plugin: 'maven-publish'
group 'com.github.hemanthsridhar'

sourceCompatibility = 1.8


dependencies {
    compile project(':gradle-artifact-add')
    testCompile group: 'org.testng', name: 'testng', version: '7.3.0'
}


plugins {
    id 'java'
}

apply plugin: 'maven-publish'
group 'artifact.new.ojo'

sourceCompatibility = 1.8


dependencies {
    compile project(':artifact-add')
    testCompile group: 'org.testng', name: 'testng', version: '7.3.0'

}